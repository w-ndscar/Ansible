---
- name: Install MSI using cmd
  hosts: win
  gather_facts: false
  tasks:
    - name: Silent install PDF Sam MSI using cmd
      ansible.windows.win_command:
        cmd: msiexec.exe /i "C:\pdfsam-5.1.1.msi" /QN
      